---
phase: 02-design-system-typeform-carousel
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/routes/__root.tsx
  - src/routes/login.tsx
  - src/routes/index.tsx
  - src/routes/journey/$id.tsx
  - src/routes/journey/$id/index.tsx
  - src/routes/journey/$id/phase1.tsx
  - src/routes/journey/$id/phase2.tsx
  - src/routes/journey/$id/phase3.new.tsx
  - src/routes/journey/$id/phase3.$entryId.tsx
  - src/routes/journey/$id/compare.tsx
  - src/components/shared/likert-scale.tsx
  - src/components/shared/rating-slider.tsx
  - src/components/shared/vas-slider.tsx
  - src/components/shared/free-text-prompt.tsx
  - src/components/shared/collapsible-section.tsx
  - src/components/shared/save-indicator.tsx
autonomous: false
requirements:
  - DSGN-04
  - DSGN-03

must_haves:
  truths:
    - "All existing pages render with dark luxury palette, no legacy cool-toned remnants"
    - "Buttons use rounded-[16px], cards use rounded-[20px], dialog uses rounded-[20px]"
    - "No accent-cool indigo references remain -- secondary actions use text-text-secondary, primary CTAs use accent-warm"
    - "All interactive elements have visible focus states using accent-warm ring"
    - "Touch targets on Likert/Rating buttons are minimum 48px"
  artifacts:
    - path: "src/routes/__root.tsx"
      provides: "Restyled root layout with dark luxury border-radius and warm tones"
    - path: "src/routes/login.tsx"
      provides: "Restyled login page with 16px input/button radius"
    - path: "src/routes/index.tsx"
      provides: "Restyled journey list with 20px card radius"
    - path: "src/routes/journey/$id/index.tsx"
      provides: "Restyled journey overview with warm secondary action colors"
    - path: "src/components/shared/likert-scale.tsx"
      provides: "Restyled likert with 48px touch targets and 16px button radius"
  key_links:
    - from: "src/index.css @theme tokens"
      to: "all files using bg-background, text-text-primary, etc."
      via: "Tailwind v4 semantic token cascade"
      pattern: "bg-background|text-text-primary|bg-surface|bg-card"
---

<objective>
Restyle all existing pages and shared components to match the dark luxury aesthetic. The token swap in Plan 01 handles ~80% of colors automatically. This plan handles the remaining ~20%: border-radius updates (per D-08), accent-cool->secondary color migration (per RESEARCH pitfall #2), typography sizing adjustments (per D-06), box-shadow removal (per D-09), and touch target increases.

Purpose: Eliminate all legacy light-theme remnants so the entire app wears the dark luxury aesthetic before the carousel is built.

Output: All 16 existing files restyled. No cool-toned classes, no legacy radius values, no box shadows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system-typeform-carousel/02-CONTEXT.md
@.planning/phases/02-design-system-typeform-carousel/02-RESEARCH.md
@.planning/phases/02-design-system-typeform-carousel/02-01-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle root layout, login, and journey list pages</name>
  <files>src/routes/__root.tsx, src/routes/login.tsx, src/routes/index.tsx</files>
  <read_first>src/routes/__root.tsx, src/routes/login.tsx, src/routes/index.tsx, src/index.css, DESIGN.md</read_first>
  <action>
Update these three route files with dark luxury border-radius, warm accent colors, and typography adjustments.

**src/routes/__root.tsx** (113 lines):
- DisclaimerDialog inner card: change `rounded-lg` to `rounded-[20px]`
- DisclaimerDialog "I understand" button: change `rounded` to `rounded-[16px]`
- Header: change `border-surface` to `border-border` (uses new warm border token)
- Header brand link "Inner Compass": keep `text-accent-warm` (now Pinterest Red)
- No other changes needed -- semantic token classes handle colors automatically

**src/routes/login.tsx** (114 lines):
- Both `<input>` elements: change `rounded` to `rounded-[16px]`, change `border-surface` to `border-border`, change `focus:ring-accent-warm` to `focus:ring-focus` (uses new focus token)
- Submit button: change `rounded` to `rounded-[16px]`
- The `text-accent-warm` on headings and links stays -- it now resolves to Pinterest Red via token swap

**src/routes/index.tsx** (227 lines):
- CreateJourneyForm input: change `rounded` to `rounded-[16px]`, `border-surface` to `border-border`
- CreateJourneyForm "Create" button: change `rounded` to `rounded-[16px]`
- DeleteButton confirm input: change `rounded` to `rounded-[16px]`, `border-surface` to `border-border`
- DeleteButton "Delete" confirm button: change `rounded` to `rounded-[16px]`
- JourneyCard: change `rounded-lg` to `rounded-[20px]`, change `border-surface` to `border-border`
- ImportButton: change `rounded` to `rounded-[16px]`, `border-surface` to `border-border`
- "New Journey" button: change `rounded` to `rounded-[16px]`

Do NOT add any box-shadow classes anywhere (per D-09).
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/__root.tsx contains `rounded-[20px]` (disclaimer card)
    - src/routes/__root.tsx contains `border-border` (header)
    - src/routes/__root.tsx does NOT contain `rounded-lg` in the DisclaimerDialog section
    - src/routes/login.tsx contains `rounded-[16px]` (inputs and buttons)
    - src/routes/login.tsx contains `border-border`
    - src/routes/index.tsx contains `rounded-[20px]` (JourneyCard)
    - src/routes/index.tsx contains `rounded-[16px]` (buttons and inputs)
    - src/routes/index.tsx contains `border-border`
    - No file contains `box-shadow` or `shadow-` classes
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>Root layout, login, and journey list pages restyled with dark luxury border-radius (16px buttons/inputs, 20px cards), warm border tokens, and no box shadows. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle journey detail pages and fix accent-cool references</name>
  <files>src/routes/journey/$id/index.tsx, src/routes/journey/$id/phase1.tsx, src/routes/journey/$id/phase2.tsx, src/routes/journey/$id/phase3.new.tsx, src/routes/journey/$id/phase3.$entryId.tsx, src/routes/journey/$id/compare.tsx</files>
  <read_first>src/routes/journey/$id/index.tsx, src/routes/journey/$id/phase1.tsx, src/routes/journey/$id/phase2.tsx, src/routes/journey/$id/phase3.new.tsx, src/routes/journey/$id/phase3.$entryId.tsx, src/routes/journey/$id/compare.tsx</read_first>
  <action>
Update all journey detail route files with dark luxury treatment. The critical change is migrating `accent-cool` references to appropriate warm alternatives.

**src/routes/journey/$id/index.tsx** (185 lines):
- PhaseBox: change `rounded-lg` to `rounded-[20px]`, `border-surface` to `border-border`
- PhaseBox link: change `text-accent-cool` to `text-text-secondary hover:text-accent-warm` (secondary action pattern -- links should be warm gray, not red, per RESEARCH pitfall #2)
- Phase3Section container: change `rounded-lg` to `rounded-[20px]`, `border-surface` to `border-border`
- Phase3Section "New Check-in" button: change `bg-accent-cool` to `bg-accent-warm` (this IS a primary CTA, so red is correct)
- Phase3Section entry links: change `rounded` to `rounded-[16px]`
- "View comparison" link: change `text-accent-cool` to `text-text-secondary hover:text-accent-warm` (secondary action)

**src/routes/journey/$id/phase1.tsx** (447 lines -- largest file):
- Search for ALL `accent-cool` references (expect ~6). For each:
  - If it is a primary CTA button: change to `accent-warm`
  - If it is a secondary link/action: change to `text-text-secondary hover:text-accent-warm`
  - If it is a focus ring: change to `focus:ring-focus`
- Update any `rounded` or `rounded-lg` on buttons to `rounded-[16px]`
- Update any card containers to `rounded-[20px]`
- Update any `border-surface` to `border-border`

**src/routes/journey/$id/phase2.tsx** (16 lines -- likely a stub):
- Apply same pattern: `accent-cool` -> appropriate warm alternative, `rounded` updates

**src/routes/journey/$id/phase3.new.tsx** (16 lines -- likely a stub):
- Apply same pattern

**src/routes/journey/$id/phase3.$entryId.tsx** (15 lines -- likely a stub):
- Apply same pattern

**src/routes/journey/$id/compare.tsx** (16 lines -- likely a stub):
- Apply same pattern

The rule for `accent-cool` migration: Pinterest Red (`accent-warm`) is reserved for PRIMARY CTAs only. Secondary actions (links, "View comparison", "Review ->") use `text-text-secondary hover:text-accent-warm` or `text-text-secondary hover:text-text-primary`. This prevents the "too much red" problem described in RESEARCH pitfall #2.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build && grep -r "accent-cool" src/routes/ || echo "NO_ACCENT_COOL_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "accent-cool" src/routes/` returns NO matches (all migrated)
    - src/routes/journey/$id/index.tsx contains `rounded-[20px]` (PhaseBox, Phase3Section)
    - src/routes/journey/$id/index.tsx contains `border-border`
    - src/routes/journey/$id/index.tsx contains `bg-accent-warm` (New Check-in button)
    - src/routes/journey/$id/index.tsx does NOT contain `text-accent-cool`
    - src/routes/journey/$id/index.tsx does NOT contain `bg-accent-cool`
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>All journey detail pages restyled. Zero accent-cool references remain in route files. Secondary actions use warm gray, primary CTAs use accent-warm (Pinterest Red). Cards use 20px radius, buttons use 16px radius. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle all shared components with dark luxury treatment</name>
  <files>src/components/shared/likert-scale.tsx, src/components/shared/rating-slider.tsx, src/components/shared/vas-slider.tsx, src/components/shared/free-text-prompt.tsx, src/components/shared/collapsible-section.tsx, src/components/shared/save-indicator.tsx</files>
  <read_first>src/components/shared/likert-scale.tsx, src/components/shared/rating-slider.tsx, src/components/shared/vas-slider.tsx, src/components/shared/free-text-prompt.tsx, src/components/shared/collapsible-section.tsx, src/components/shared/save-indicator.tsx</read_first>
  <action>
Update all 6 shared components for dark luxury aesthetic. These components are used by future phase forms, so they must look correct.

**src/components/shared/likert-scale.tsx** (56 lines):
- Option buttons: change any `rounded-lg` to `rounded-[16px]`
- Touch targets: change `min-h-[44px]` to `min-h-[48px]` and add `min-w-[48px]` (per CRSL-06, D-15)
- If `accent-cool` is used for selected state, change to `accent-warm`
- Add visible focus state: `focus:ring-2 focus:ring-focus focus:ring-offset-2 focus:ring-offset-background`

**src/components/shared/rating-slider.tsx** (44 lines):
- Option buttons: change any `rounded-lg` to `rounded-[16px]`
- Touch targets: change to `min-h-[48px] min-w-[48px]`
- If `accent-cool` is used for selected state, change to `accent-warm`
- Add visible focus state: `focus:ring-2 focus:ring-focus focus:ring-offset-2 focus:ring-offset-background`

**src/components/shared/vas-slider.tsx** (49 lines):
- Slider track: update any hardcoded or `accent-cool` colors to `accent-warm` for the filled portion
- Touch target on thumb: ensure minimum 48px
- Add `rounded-[16px]` to any container elements

**src/components/shared/free-text-prompt.tsx** (46 lines):
- Textarea: change `rounded` to `rounded-[16px]`, `border-surface` to `border-border`
- Focus ring: change to `focus:ring-focus` (uses new Pinterest Red focus token)

**src/components/shared/collapsible-section.tsx** (83 lines):
- Container: change any `rounded-lg` or `rounded-xl` to `rounded-[20px]` (card-level)
- If `accent-cool` is used (expect ~2 references), change to `accent-warm` for expand/collapse indicator or to `text-text-secondary` for non-primary elements
- Border: `border-surface` to `border-border`

**src/components/shared/save-indicator.tsx** (15 lines):
- Minimal changes expected -- likely just uses token classes. Verify no cool-toned hardcoded values.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build && grep -r "accent-cool" src/components/shared/ || echo "NO_ACCENT_COOL_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "accent-cool" src/components/shared/` returns NO matches
    - src/components/shared/likert-scale.tsx contains `min-h-[48px]`
    - src/components/shared/likert-scale.tsx contains `rounded-[16px]`
    - src/components/shared/rating-slider.tsx contains `min-h-[48px]`
    - src/components/shared/rating-slider.tsx contains `rounded-[16px]`
    - src/components/shared/free-text-prompt.tsx contains `rounded-[16px]`
    - src/components/shared/free-text-prompt.tsx contains `border-border`
    - src/components/shared/collapsible-section.tsx contains `rounded-[20px]`
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>All 6 shared components restyled with dark luxury treatment. Zero accent-cool references. Touch targets at 48px minimum. Buttons 16px radius, cards 20px radius. Visible focus states on all interactive elements. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of dark luxury aesthetic</name>
  <files>none</files>
  <action>
Human verifies the dark luxury aesthetic overhaul across all pages.
What was built: Complete dark luxury aesthetic overhaul of all existing pages and components. Token swap (Plan 01) changed all semantic colors automatically. This plan updated border-radius, accent-cool migration, touch targets, and focus states across 16 files.
How to verify:
1. Run `bun dev` and open http://localhost:5173
2. Login page: Verify warm near-black background (#111110), Inter font rendering, Pinterest Red accent on heading and button, 16px rounded inputs and button, no cool-toned elements
3. Journey list (/): Verify 20px rounded journey cards with warm border, Pinterest Red "New Journey" button with 16px radius, warm gray text hierarchy
4. Journey detail (/journey/[id]): Verify phase boxes have 20px radius, "New Check-in" button is Pinterest Red, secondary links ("View comparison", "Review ->") are warm gray (not red)
5. Phase 1 form: Verify all Likert scale buttons have 48px touch targets, 16px radius, warm selected state (not indigo)
6. Resize to 375px mobile: Verify no overflow, all touch targets visually large enough
7. Tab through the login form: Verify focus rings are visible (Pinterest Red) on dark background
8. Check: NO cool blue/indigo/navy remnants anywhere. Everything warm-toned.
  </action>
  <verify>User approves visual appearance</verify>
  <done>User has confirmed the dark luxury aesthetic is correct across all pages</done>
</task>

</tasks>

<verification>
- `bun run build` passes
- `grep -r "accent-cool" src/routes/ src/components/shared/` returns 0 matches
- `grep -r "slate-\|gray-\|zinc-\|#0f172a\|#1e293b\|#334155" src/` returns 0 matches (no cool colors anywhere)
- `grep -c "rounded-\[16px\]" src/routes/login.tsx` returns 3+ (inputs and button)
- `grep -c "rounded-\[20px\]" src/routes/index.tsx` returns 1+ (journey card)
- `grep -c "min-h-\[48px\]" src/components/shared/likert-scale.tsx` returns 1+
</verification>

<success_criteria>
- Every existing page renders in dark luxury aesthetic with warm tones only
- All accent-cool references eliminated from routes and components
- Buttons use 16px radius, cards use 20px radius
- Touch targets on interactive instrument components are 48px minimum
- Focus states visible on dark background using Pinterest Red
- No box shadows anywhere
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-typeform-carousel/02-02-SUMMARY.md`
</output>
