---
phase: 02-design-system-typeform-carousel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/hooks/use-reduced-motion.ts
  - src/hooks/index.ts
  - index.html
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-05

must_haves:
  truths:
    - "App background is warm near-black (#111110), not cool navy"
    - "Text renders in Inter font with warm near-white (#ede9e3) color"
    - "Border radius tokens exist at 12px, 16px, 20px, 28px, 40px"
    - "Type scale tokens exist (--text-xs: 12px, --text-sm: 14px, --text-base: 16px, --text-heading: 28px)"
    - "Heading letter-spacing is negative (-0.05em) per D-06"
    - "useReducedMotion hook is available for carousel consumption"
  artifacts:
    - path: "src/index.css"
      provides: "Dark luxury @theme tokens, Inter font import, body styles, type scale, letter-spacing"
      contains: "--color-background: #111110"
    - path: "src/hooks/use-reduced-motion.ts"
      provides: "Reduced motion preference detection hook"
      exports: ["useReducedMotion"]
    - path: "index.html"
      provides: "Google Fonts preconnect for fast font loading"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "src/index.css"
      to: "all components using bg-background, text-text-primary, etc."
      via: "Tailwind v4 @theme token cascade"
      pattern: "--color-background: #111110"
    - from: "src/hooks/use-reduced-motion.ts"
      to: "src/hooks/index.ts"
      via: "barrel re-export"
      pattern: "export.*useReducedMotion.*from.*use-reduced-motion"
---

<objective>
Replace the existing navy-based design tokens with the dark luxury palette, set up Inter typography via Google Fonts, define border-radius tokens, add compact type scale tokens (per D-06), and create the useReducedMotion hook needed by the carousel.

Purpose: This is the foundation for the entire phase. Changing @theme values cascades to ~80% of the visual update automatically through existing semantic Tailwind classes. The reduced motion hook is a prerequisite for the carousel's accessibility.

Output: Updated src/index.css with dark luxury tokens, Inter font loading, radius tokens, type scale tokens. New useReducedMotion hook. Google Fonts preconnect in index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-typeform-carousel/02-CONTEXT.md
@.planning/phases/02-design-system-typeform-carousel/02-RESEARCH.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite @theme tokens, type scale, and set up Inter font loading</name>
  <files>src/index.css, index.html</files>
  <read_first>src/index.css, index.html, DESIGN.md</read_first>
  <action>
Rewrite `src/index.css` completely with the following content:

1. Add Google Fonts import at the top: `@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap");`
2. Keep `@import "tailwindcss";` after the font import.
3. Replace the entire `@theme` block with these exact tokens (per D-01, D-02, D-03, D-05, D-06, D-08):

```
@theme {
  --color-background: #111110;
  --color-surface: #1c1c1a;
  --color-card: #2a2a27;
  --color-text-primary: #ede9e3;
  --color-text-secondary: #91918c;
  --color-accent-warm: #e60023;
  --color-accent-cool: #e60023;
  --color-success: #34d399;
  --color-warning: #fb923c;
  --color-danger: #f87171;
  --color-border: #3a3a36;
  --color-border-hover: #4a4a44;
  --color-focus: #e60023;

  --default-font-family: "Inter", -apple-system, system-ui, "Segoe UI", Roboto, sans-serif;
  --font-sans: "Inter", -apple-system, system-ui, "Segoe UI", Roboto, sans-serif;

  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-heading: 28px;

  --tracking-heading: -0.05em;

  --radius-sm: 12px;
  --radius-md: 16px;
  --radius-lg: 20px;
  --radius-xl: 28px;
  --radius-2xl: 40px;
}
```

4. After the @theme block, add a CSS rule for heading letter-spacing (per D-06):
```css
h2, h3 {
  letter-spacing: var(--tracking-heading);
}
```

5. Keep the body rule: `body { background-color: var(--color-background); color: var(--color-text-primary); }`

6. In `index.html`, add preconnect hints in the `<head>` BEFORE any existing links/scripts:
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
```

Per D-07: preload only critical weights (400, 600) via the Google Fonts URL. The `display=swap` parameter ensures text renders immediately with fallback font.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `--color-background: #111110;`
    - src/index.css contains `--color-surface: #1c1c1a;`
    - src/index.css contains `--color-card: #2a2a27;`
    - src/index.css contains `--color-text-primary: #ede9e3;`
    - src/index.css contains `--color-text-secondary: #91918c;`
    - src/index.css contains `--color-accent-warm: #e60023;`
    - src/index.css contains `--color-accent-cool: #e60023;`
    - src/index.css contains `--color-border: #3a3a36;`
    - src/index.css contains `--default-font-family: "Inter"`
    - src/index.css contains `--text-xs: 12px;`
    - src/index.css contains `--text-sm: 14px;`
    - src/index.css contains `--text-base: 16px;`
    - src/index.css contains `--text-heading: 28px;`
    - src/index.css contains `--tracking-heading: -0.05em;`
    - src/index.css contains `letter-spacing: var(--tracking-heading)`
    - src/index.css contains `--radius-md: 16px;`
    - src/index.css contains `--radius-lg: 20px;`
    - src/index.css contains `--radius-2xl: 40px;`
    - src/index.css contains `fonts.googleapis.com/css2?family=Inter`
    - index.html contains `fonts.googleapis.com`
    - index.html contains `fonts.gstatic.com`
    - src/index.css does NOT contain `#0f172a`
    - src/index.css does NOT contain `#1e293b`
    - src/index.css does NOT contain `#334155`
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>All @theme tokens replaced with dark luxury palette. Inter font loaded via Google Fonts with preconnect. Type scale tokens (12-28px) and heading letter-spacing (-0.05em) added per D-06. Old navy colors completely removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create useReducedMotion hook and export it</name>
  <files>src/hooks/use-reduced-motion.ts, src/hooks/index.ts</files>
  <read_first>src/hooks/index.ts, src/hooks/use-auto-save.ts</read_first>
  <action>
Create `src/hooks/use-reduced-motion.ts` with the following implementation (per D-11, CRSL-05):

```typescript
import { useState, useEffect } from 'react'

const QUERY = '(prefers-reduced-motion: no-preference)'

export function useReducedMotion(): boolean {
  const [prefersReducedMotion, setPrefersReducedMotion] = useState(true)

  useEffect(() => {
    const mql = window.matchMedia(QUERY)
    setPrefersReducedMotion(!mql.matches)

    const handler = (event: MediaQueryListEvent) => {
      setPrefersReducedMotion(!event.matches)
    }
    mql.addEventListener('change', handler)
    return () => mql.removeEventListener('change', handler)
  }, [])

  return prefersReducedMotion
}
```

The hook defaults to `true` (reduced motion) on first render to be safe for SSR/hydration, then reads the actual preference in useEffect.

Update `src/hooks/index.ts` to add the export. Follow existing pattern in the file (check whether it uses barrel re-exports or named exports). Add: `export { useReducedMotion } from './use-reduced-motion'`
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-reduced-motion.ts exists
    - src/hooks/use-reduced-motion.ts contains `export function useReducedMotion(): boolean`
    - src/hooks/use-reduced-motion.ts contains `prefers-reduced-motion: no-preference`
    - src/hooks/use-reduced-motion.ts contains `addEventListener`
    - src/hooks/use-reduced-motion.ts contains `removeEventListener`
    - src/hooks/index.ts contains `useReducedMotion`
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>useReducedMotion hook created and exported. Defaults to true (safe for initial render). Listens for system preference changes. Barrel re-export wired in src/hooks/index.ts. Build passes.</done>
</task>

</tasks>

<verification>
- `bun run build` passes (TypeScript type-check + Vite production build)
- `grep -c "#0f172a\|#1e293b\|#334155" src/index.css` returns 0 (no legacy navy colors)
- `grep -c "#111110" src/index.css` returns 1+ (dark luxury background present)
- `grep -c "text-heading" src/index.css` returns 1+ (type scale present)
- `grep -c "tracking-heading" src/index.css` returns 1+ (letter-spacing present)
- `grep -c "useReducedMotion" src/hooks/use-reduced-motion.ts` returns 1+
- `grep -c "useReducedMotion" src/hooks/index.ts` returns 1+
</verification>

<success_criteria>
- Dark luxury palette tokens are the sole color definitions in @theme
- Inter font is loaded and set as default font family
- Type scale tokens (--text-xs: 12px, --text-sm: 14px, --text-base: 16px, --text-heading: 28px) defined in @theme
- Heading letter-spacing token (--tracking-heading: -0.05em) defined and applied to h2, h3
- Border-radius tokens (12px, 16px, 20px, 28px, 40px) are defined in @theme
- useReducedMotion hook exists and is exported from hooks barrel
- Production build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-typeform-carousel/02-01-SUMMARY.md`
</output>
