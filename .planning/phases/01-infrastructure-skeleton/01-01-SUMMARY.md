---
phase: 01-infrastructure-skeleton
plan: 01
subsystem: infra
tags: [vercel, ai-sdk, anthropic, vite-proxy, typescript-project-references]

requires:
  - phase: none
    provides: existing Vite + React + Supabase SPA scaffold
provides:
  - AI SDK dependencies (ai, @ai-sdk/anthropic) installed
  - Serverless function TypeScript config (api/tsconfig.json)
  - Vite dev proxy forwarding /api to localhost:3001
  - Vercel deployment config with SPA rewrite
  - Server-side env var documentation in .env.example
affects: [01-02-api-chat-endpoint]

tech-stack:
  added: [ai@6.0.156, "@ai-sdk/anthropic@3.0.68", vercel@50.43.0]
  patterns: [vercel-serverless-functions, vite-dev-proxy, typescript-project-references]

key-files:
  created: [api/tsconfig.json, api/_placeholder.ts, vercel.json]
  modified: [package.json, bun.lock, tsconfig.json, vite.config.ts, .env.example, .gitignore]

key-decisions:
  - "api/tsconfig.json uses bundler moduleResolution with Node.js types only (no DOM)"
  - "Vite proxy uses /api key without path rewrite -- Vercel function paths match directly"
  - "Added api/_placeholder.ts to satisfy tsc -b project references until api/chat.ts is created in Plan 02"

patterns-established:
  - "Server-side env vars use no VITE_ prefix (ANTHROPIC_API_KEY, SUPABASE_SERVICE_ROLE_KEY)"
  - "api/ directory has separate tsconfig for Node.js serverless functions"

requirements-completed: [INFRA-02, INFRA-03]

duration: 2min
completed: 2026-04-10
---

# Phase 01 Plan 01: Infrastructure Skeleton Summary

**AI SDK deps, Vercel serverless config, and Vite dev proxy for /api forwarding to localhost:3001**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-10T11:42:08Z
- **Completed:** 2026-04-10T11:44:16Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Installed ai and @ai-sdk/anthropic dependencies for Claude Haiku integration
- Created separate TypeScript config for api/ serverless functions with Node.js types
- Added Vite dev proxy so /api/* requests forward to vercel dev on port 3001
- Created vercel.json with SPA rewrite rule for deployment
- Documented all server-side env vars in .env.example without VITE_ prefix

## Task Commits

Each task was committed atomically:

1. **Task 1: Install dependencies and create config files** - `0791df9` (feat)
2. **Task 2: Add Vite dev proxy for /api/* forwarding** - `393f0bf` (feat)

**Auto-fix commits:**
3. **Fix: api/ placeholder for tsc project references** - `0a41564` (fix)
4. **Chore: gitignore tsbuildinfo files** - `965759c` (chore)

## Files Created/Modified
- `api/tsconfig.json` - TypeScript config for serverless functions with Node.js types
- `api/_placeholder.ts` - Temporary placeholder until api/chat.ts is created in Plan 02
- `vercel.json` - SPA rewrite rule for Vercel deployment
- `vite.config.ts` - Added server.proxy config for /api to localhost:3001
- `tsconfig.json` - Added api/ as project reference
- `package.json` - Added ai, @ai-sdk/anthropic deps and vercel devDep
- `.env.example` - Added ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
- `.gitignore` - Added *.tsbuildinfo

## Decisions Made
- api/tsconfig.json uses bundler moduleResolution with Node.js types only (no DOM) to keep serverless functions isolated from client-side types
- Vite proxy uses /api key without path rewrite since Vercel function paths match directly
- Added api/_placeholder.ts to satisfy tsc -b project references until api/chat.ts is created in Plan 02

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Empty api/ directory caused TS18003 build error**
- **Found during:** Overall verification (bun run build)
- **Issue:** tsc -b failed because api/tsconfig.json included "." but the directory had no .ts files
- **Fix:** Created api/_placeholder.ts with a bare export to satisfy TypeScript
- **Files modified:** api/_placeholder.ts
- **Verification:** bun run build succeeds
- **Committed in:** 0a41564

**2. [Rule 3 - Blocking] Untracked api/tsconfig.tsbuildinfo generated by build**
- **Found during:** Post-commit untracked file check
- **Issue:** tsc -b generates .tsbuildinfo files that should not be committed
- **Fix:** Added *.tsbuildinfo to .gitignore
- **Files modified:** .gitignore
- **Verification:** git status shows no untracked files
- **Committed in:** 965759c

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary for clean builds. No scope creep.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required

External services require manual configuration before the AI chat endpoint (Plan 02) will function:
- **ANTHROPIC_API_KEY** - Anthropic Console -> API Keys -> Create Key
- **SUPABASE_URL** - Same value as VITE_SUPABASE_URL (without VITE_ prefix for server-side)
- **SUPABASE_SERVICE_ROLE_KEY** - Supabase Dashboard -> Settings -> API -> service_role key (secret)

## Next Phase Readiness
- All config files in place for Plan 02 to create api/chat.ts immediately
- Dependencies installed and verified
- Build passes cleanly

## Known Stubs
- `api/_placeholder.ts` - Empty placeholder, will be replaced by api/chat.ts in Plan 01-02

## Self-Check: PASSED

All 4 key files verified present. All 4 commit hashes verified in git log.

---
*Phase: 01-infrastructure-skeleton*
*Completed: 2026-04-10*
