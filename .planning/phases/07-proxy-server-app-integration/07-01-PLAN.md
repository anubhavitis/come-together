---
phase: 07-proxy-server-app-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/chat.ts
  - .env.example
  - docs/PROXY-SETUP.md
autonomous: false
requirements:
  - PROXY-01
  - PROXY-02
  - PROXY-03
  - APINT-01
  - APINT-02
  - APINT-03
user_setup:
  - service: claude-code-proxy
    why: "Proxy server translates Anthropic API format to OpenAI format for backend model routing"
    env_vars:
      - name: ANTHROPIC_BASE_URL
        source: "URL of deployed proxy (e.g., http://localhost:8082/v1 for local, or https://your-proxy.railway.app/v1 for hosted)"
    dashboard_config:
      - task: "Clone and run claude-code-proxy"
        location: "https://github.com/fuergaosi233/claude-code-proxy"

must_haves:
  truths:
    - "When ANTHROPIC_BASE_URL is set, AI requests route through that URL instead of api.anthropic.com"
    - "When ANTHROPIC_BASE_URL is not set, AI requests go directly to Anthropic (unchanged behavior)"
    - ".env.example documents the new ANTHROPIC_BASE_URL variable with clear instructions"
    - "A setup guide exists explaining how to clone, configure, and run the proxy"
  artifacts:
    - path: "api/chat.ts"
      provides: "Configurable Anthropic provider with optional base URL override"
      contains: "createAnthropic"
    - path: ".env.example"
      provides: "Documentation of ANTHROPIC_BASE_URL env var"
      contains: "ANTHROPIC_BASE_URL"
    - path: "docs/PROXY-SETUP.md"
      provides: "Step-by-step proxy setup guide"
      contains: "claude-code-proxy"
  key_links:
    - from: "api/chat.ts"
      to: "process.env.ANTHROPIC_BASE_URL"
      via: "createAnthropic({ baseURL })"
      pattern: "createAnthropic.*baseURL.*ANTHROPIC_BASE_URL"
---

<objective>
Make the app's AI layer route through a configurable proxy by swapping the Anthropic provider import in `api/chat.ts` and documenting the proxy setup.

Purpose: Enable routing AI requests through claude-code-proxy so alternative LLM backends (OpenAI, Azure, Ollama, etc.) can be used without changing app code.
Output: Updated `api/chat.ts` with configurable base URL, updated `.env.example`, and a proxy setup guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-proxy-server-app-integration/07-CONTEXT.md
@.planning/research/PROXY-RESEARCH.md

<interfaces>
<!-- Current api/chat.ts import that needs changing -->
From api/chat.ts (line 3):
```typescript
import { anthropic } from '@ai-sdk/anthropic'
```

Usage (line 128):
```typescript
model: anthropic('claude-3-5-haiku-20241022'),
```

<!-- Target pattern from research -->
```typescript
import { createAnthropic } from '@ai-sdk/anthropic'

const anthropic = createAnthropic({
  baseURL: process.env.ANTHROPIC_BASE_URL || 'https://api.anthropic.com/v1',
  apiKey: process.env.ANTHROPIC_API_KEY || '',
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update api/chat.ts with configurable Anthropic provider and document env var</name>
  <files>api/chat.ts, .env.example</files>
  <read_first>api/chat.ts, .env.example</read_first>
  <action>
In `api/chat.ts`, make these changes (per D-05, D-06, D-07, D-08, D-10):

1. Replace line 3 `import { anthropic } from '@ai-sdk/anthropic'` with `import { createAnthropic } from '@ai-sdk/anthropic'`

2. Add a configured provider instance AFTER the Supabase client setup (after line 14) and BEFORE the system prompt constants:

```typescript
const anthropic = createAnthropic({
  baseURL: process.env.ANTHROPIC_BASE_URL || 'https://api.anthropic.com/v1',
  apiKey: process.env.ANTHROPIC_API_KEY || '',
})
```

3. Keep the existing `apiKey` validation block (lines 118-124) in the POST handler unchanged. It provides a clear 500 error message when the key is missing. The provider creation at module level uses `|| ''` which will cause the SDK to throw later, but the explicit check gives a better error message. Leave it for backward compatibility (per D-08).

Summary of changes to `api/chat.ts`:
- Line 3: `import { anthropic }` becomes `import { createAnthropic }`
- Add after line 14: `const anthropic = createAnthropic({ ... })` block (3 lines)
- Everything else stays exactly the same -- the `anthropic('claude-3-5-haiku-20241022')` call on line 128 now refers to the configured provider instance instead of the imported default

In `.env.example`, add after the existing `ANTHROPIC_API_KEY` line (per D-11):

```
# Optional: Route AI requests through a proxy server instead of directly to Anthropic.
# Set this to your proxy's base URL (e.g., http://localhost:8082/v1 or https://your-proxy.railway.app/v1).
# When unset, requests go directly to https://api.anthropic.com/v1 (default behavior).
# See docs/PROXY-SETUP.md for proxy configuration instructions.
ANTHROPIC_BASE_URL=
```
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && grep -q "createAnthropic" api/chat.ts && grep -q "ANTHROPIC_BASE_URL" api/chat.ts && grep -q "ANTHROPIC_BASE_URL" .env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `api/chat.ts` imports `createAnthropic` (not `anthropic`) from `@ai-sdk/anthropic`
- `api/chat.ts` creates a provider instance using `process.env.ANTHROPIC_BASE_URL` with fallback to `https://api.anthropic.com/v1`
- `api/chat.ts` still uses `anthropic('claude-3-5-haiku-20241022')` for the model call (variable name unchanged)
- `.env.example` contains `ANTHROPIC_BASE_URL` with explanatory comments
- `bun run build` passes (no type errors from the import change)
  </acceptance_criteria>
  <done>api/chat.ts uses createAnthropic with configurable baseURL; .env.example documents the new variable; build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create proxy setup guide</name>
  <files>docs/PROXY-SETUP.md</files>
  <read_first>api/chat.ts, .planning/research/PROXY-RESEARCH.md</read_first>
  <action>
Create `docs/PROXY-SETUP.md` with a step-by-step guide covering (per D-01, D-02, D-03, D-04):

**Structure:**
1. **Overview** -- What the proxy does (translates Anthropic API format to OpenAI format), when you need it (routing to non-Anthropic backends), architecture diagram from research
2. **Quick Start (Local Development)** -- Clone repo, install Python deps, configure env vars, run with uvicorn, verify with health check
3. **Environment Variables (Proxy Server)** -- Table of all proxy env vars with descriptions:
   - `OPENAI_API_KEY` (required) -- API key for the backend provider
   - `OPENAI_BASE_URL` (required) -- Backend endpoint URL
   - `SMALL_MODEL` (optional, default: gpt-4o-mini) -- Model for "haiku" requests
   - `BIG_MODEL` (optional) -- Model for "opus" requests
   - `MIDDLE_MODEL` (optional) -- Model for "sonnet" requests
   - `ANTHROPIC_API_KEY` (optional) -- Shared secret for client auth validation
   - `PORT` (optional, default: 8082) -- Server port
4. **Connecting the App** -- Set `ANTHROPIC_BASE_URL` in app's env to proxy URL, verify with a test conversation
5. **Production Deployment** -- Brief notes on Railway and Fly.io deployment (per D-03, document but don't require)
6. **Troubleshooting** -- Common issues: proxy unreachable (check URL), auth mismatch (check API key), model routing (check SMALL_MODEL config)
7. **Important Notes** -- The proxy converts Claude-to-OpenAI format. If backend IS Anthropic, proxy is unnecessary (adds overhead). Score extraction may behave differently with non-Claude models.

Keep the guide concise and practical. No marketing language. Include actual commands.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && test -f docs/PROXY-SETUP.md && grep -q "claude-code-proxy" docs/PROXY-SETUP.md && grep -q "SMALL_MODEL" docs/PROXY-SETUP.md && grep -q "OPENAI_API_KEY" docs/PROXY-SETUP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `docs/PROXY-SETUP.md` exists
- Contains clone instructions for https://github.com/fuergaosi233/claude-code-proxy
- Documents all proxy environment variables (OPENAI_API_KEY, OPENAI_BASE_URL, SMALL_MODEL at minimum)
- Contains local development quick start with uvicorn command
- Documents how to connect the app (set ANTHROPIC_BASE_URL)
- Mentions production deployment options (Railway, Fly.io)
  </acceptance_criteria>
  <done>Comprehensive proxy setup guide exists at docs/PROXY-SETUP.md with clone, configure, run, and connect instructions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify proxy integration works end-to-end</name>
  <files>api/chat.ts, .env.example, docs/PROXY-SETUP.md</files>
  <action>
Human verifies the integration. What was built: Configurable Anthropic provider in api/chat.ts that routes through ANTHROPIC_BASE_URL when set, with proxy setup documentation.

Steps to verify:
1. Verify fallback (no proxy): Run `bun dev`, start a Phase 1 conversation. Confirm AI responds normally (direct Anthropic calls, current behavior unchanged).
2. Verify proxy routing (optional, if proxy is available):
   a. Follow `docs/PROXY-SETUP.md` to clone and run the proxy locally
   b. Set `ANTHROPIC_BASE_URL=http://localhost:8082/v1` in your `.env`
   c. Restart `bun dev`
   d. Verify the proxy health endpoint: `curl http://localhost:8082/health`
   e. Start a Phase 1 conversation and confirm AI responds through the proxy
   f. Check proxy server logs to confirm requests are being forwarded
3. Verify build: Run `bun run build` and confirm no errors.

Resume: Type "approved" if fallback works and build passes. Proxy routing verification is optional for this phase (Phase 8 covers end-to-end validation).
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- Build passes without errors
- Direct Anthropic fallback works when ANTHROPIC_BASE_URL is unset
- User confirms the integration is acceptable
  </acceptance_criteria>
  <done>User has verified the app builds and AI fallback works; proxy routing optionally verified</done>
</task>

</tasks>

<verification>
1. `grep "createAnthropic" api/chat.ts` returns a match
2. `grep "ANTHROPIC_BASE_URL" api/chat.ts` returns a match
3. `grep "ANTHROPIC_BASE_URL" .env.example` returns a match
4. `test -f docs/PROXY-SETUP.md` succeeds
5. `bun run build` passes without errors
</verification>

<success_criteria>
- api/chat.ts uses a configurable Anthropic provider that respects ANTHROPIC_BASE_URL
- When ANTHROPIC_BASE_URL is unset, behavior is identical to current (direct Anthropic calls)
- When ANTHROPIC_BASE_URL is set, requests route to that URL
- .env.example documents the new variable
- A setup guide explains how to run the proxy
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-proxy-server-app-integration/07-01-SUMMARY.md`
</output>
