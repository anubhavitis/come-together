---
phase: 06-comparison-sessions-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scoring.ts
  - src/components/charts/meq30-radar.tsx
  - src/components/charts/swemwbs-bar.tsx
  - src/components/charts/edi-gauge.tsx
  - src/components/charts/ebi-gauge.tsx
  - src/routes/journey/$id/compare.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "Comparison view shows MEQ-30 radar chart with 4 subscale axes and 3.0 threshold line"
    - "Comparison view shows SWEMWBS before/after bar chart with meaningful change threshold highlighted"
    - "Comparison view shows EDI mean score and EBI sum score"
    - "AI trip summary text renders alongside charts in dual layout (side-by-side desktop, stacked mobile)"
    - "Charts use dark luxury palette — no default Recharts colors"
  artifacts:
    - path: "src/lib/scoring.ts"
      provides: "MEQ-30 subscale mean calculator and SWEMWBS total scorer"
      exports: ["computeMeq30Subscales", "computeSwemwbsTotal"]
    - path: "src/components/charts/meq30-radar.tsx"
      provides: "MEQ-30 radar chart with threshold"
    - path: "src/components/charts/swemwbs-bar.tsx"
      provides: "SWEMWBS before/after bar chart"
    - path: "src/components/charts/edi-gauge.tsx"
      provides: "EDI score display"
    - path: "src/components/charts/ebi-gauge.tsx"
      provides: "EBI score display"
    - path: "src/routes/journey/$id/compare.tsx"
      provides: "Full comparison view with charts + trip summary"
  key_links:
    - from: "src/routes/journey/$id/compare.tsx"
      to: "src/lib/scoring.ts"
      via: "import computeMeq30Subscales, computeSwemwbsTotal"
      pattern: "computeMeq30Subscales|computeSwemwbsTotal"
    - from: "src/routes/journey/$id/compare.tsx"
      to: "src/hooks/use-journeys.ts"
      via: "useJourney hook for FullJourney data"
      pattern: "useJourney"
    - from: "src/components/charts/meq30-radar.tsx"
      to: "recharts"
      via: "RadarChart import"
      pattern: "RadarChart"
---

<objective>
Build the comparison view — the payoff of the entire app — showing before/after instrument score visualizations alongside the AI trip summary.

Purpose: Users who complete all three phases see their transformation visualized through validated instrument scores and AI narrative. This is the core value delivery moment.
Output: Scoring utilities, four chart components, and the fully wired comparison route page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/journey.ts
@src/hooks/use-journeys.ts
@src/lib/scoring.ts
@src/lib/score-parser.ts
@src/routes/journey/$id/compare.tsx
@src/index.css

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/journey.ts:
```typescript
export type Meq30 = Record<'item1' | ... | 'item30', number | undefined>
export type Edi = Record<'item1' | ... | 'item8', number | undefined>
export type Ebi = Record<'item1' | ... | 'item6', number | undefined>
export type Swemwbs = Record<'item1' | ... | 'item7', number | undefined>

export type FullJourney = Journey & {
  phase1: Phase1 | null    // phase1.swemwbs = before SWEMWBS scores
  phase2: Phase2 | null    // phase2.meq30, phase2.edi, phase2.ebi
  phase3Entries: Phase3Entry[]  // phase3Entries[0].swemwbs = after SWEMWBS, tripSummary
}
```

From src/hooks/use-journeys.ts:
```typescript
export function useJourney(id: string): UseQueryResult<FullJourney>
```

From src/lib/scoring.ts:
```typescript
export function computePhase2Scores(answers, questions): { meq30: Meq30; edi: Edi; ebi: Ebi }
```

MEQ-30 Subscale Mapping (standard validated instrument):
- Mystical: items 4, 5, 6, 14, 15, 21, 29 (internal unity + external unity)
- Positive Mood: items 2, 8, 12, 18, 22, 26 (positive mood + sacredness)
- Transcendence of Time/Space: items 1, 7, 11, 16, 20, 25 (transcendence of time/space)
- Ineffability: items 3, 9, 10, 13, 17, 19, 23, 24, 27, 28, 30 (ineffability + noetic quality)

Design tokens (from src/index.css):
- Background: --color-background (#0f172a)
- Surface: --color-surface (#1e293b)
- Card: --color-card (#334155)
- Accent warm: --color-accent-warm (#f59e0b amber)
- Accent cool: --color-accent-cool (#818cf8 indigo)
- Text primary: --color-text-primary (#e2e8f0)
- Text secondary: --color-text-secondary (#94a3b8)
- Success: --color-success (#34d399)
- Warning: --color-warning (#fb923c)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoring utilities and chart components</name>
  <files>
    src/lib/scoring.ts
    src/components/charts/meq30-radar.tsx
    src/components/charts/swemwbs-bar.tsx
    src/components/charts/edi-gauge.tsx
    src/components/charts/ebi-gauge.tsx
  </files>
  <read_first>
    src/lib/scoring.ts
    src/types/journey.ts
    src/index.css
    src/lib/score-parser.ts
  </read_first>
  <action>
    **1. Add scoring utilities to `src/lib/scoring.ts`** (append to existing file):

    Add `computeMeq30Subscales(meq30: Meq30)` that returns `{ mystical: number, positiveMood: number, transcendence: number, ineffability: number }`. Each subscale is the mean of its items (treating undefined as median 3). Per D-02, these are the 4 radar axes. Use standard MEQ-30 factor mapping:
    - Mystical: items 4, 5, 6, 14, 15, 21, 29
    - Positive Mood: items 2, 8, 12, 18, 22, 26
    - Transcendence: items 1, 7, 11, 16, 20, 25
    - Ineffability: items 3, 9, 10, 13, 17, 19, 23, 24, 27, 28, 30

    Add `computeSwemwbsTotal(swemwbs: Swemwbs)` that returns `number` (sum of 7 items, range 7-35, undefined items default to 3). Per D-03, used for before/after comparison.

    Add `computeEdiMean(edi: Edi)` that returns `number` (mean of 8 items, range 0-100, undefined items default to 50). Per D-04.

    Add `computeEbiSum(ebi: Ebi)` that returns `number` (sum of 6 items, range 0-600, undefined items default to 50). Per D-05.

    **2. Create `src/components/charts/meq30-radar.tsx`** (per D-02):

    Props: `{ subscales: { mystical: number; positiveMood: number; transcendence: number; ineffability: number } }`

    Use Recharts `RadarChart`, `PolarGrid`, `PolarAngleAxis`, `Radar`, `ResponsiveContainer`. Each axis 0-5. Add a second `Radar` with constant value 3.0 at every axis as the "Complete Mystical Experience" threshold line (dashed, semi-transparent warm gray). Data points in accent-warm (#f59e0b). Background fill transparent. Grid lines in border color (#1e293b or similar). Per D-06, no default Recharts colors — override everything with dark luxury tokens. Axis labels: "Mystical", "Positive Mood", "Transcendence", "Ineffability" in text-secondary color. Use `useReducedMotion` from `@/hooks` — if reduced motion, set `isAnimationActive={false}`.

    **3. Create `src/components/charts/swemwbs-bar.tsx`** (per D-03):

    Props: `{ before: number; after: number | null }` (before = Phase 1 SWEMWBS total, after = Phase 3 SWEMWBS total or null if not complete).

    Use Recharts `BarChart`, `Bar`, `XAxis`, `YAxis`, `ResponsiveContainer`, `ReferenceLine`. Y-axis range 7-35. Two bars: "Before" in accent-cool (#818cf8), "After" in accent-warm (#f59e0b). If after is null, show only the before bar. If the delta >= 3 points, show a "Meaningful Change" label near the bars using Recharts `Label` or a custom overlay in success color (#34d399). Per D-10, highlight meaningful change threshold. Bar radius 8px for rounded tops. Background transparent, axis text in text-secondary.

    **4. Create `src/components/charts/edi-gauge.tsx`** (per D-04):

    Props: `{ score: number }` (0-100 mean).

    Simple horizontal bar gauge — not a Recharts chart, just a styled div. A rounded container (bg-card, rounded-[16px], h-8) with an inner filled bar (bg-accent-warm, rounded-[16px]) at `width: ${score}%`. Score value displayed as text centered on the bar. Label "Ego Dissolution" above. Score range "0-100" below in text-secondary. If score >= 70, add a subtle glow or use success color for "high dissolution".

    **5. Create `src/components/charts/ebi-gauge.tsx`** (per D-05):

    Props: `{ score: number }` (0-600 sum).

    Same horizontal bar gauge pattern as EDI. Container with inner bar at `width: ${(score / 600) * 100}%`. Label "Emotional Breakthrough" above. Score value displayed as text. Range "0-600" below in text-secondary. If score >= 400, use success color accent.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "computeMeq30Subscales\|computeSwemwbsTotal\|computeEdiMean\|computeEbiSum" src/lib/scoring.ts` returns 4+
    - `ls src/components/charts/meq30-radar.tsx src/components/charts/swemwbs-bar.tsx src/components/charts/edi-gauge.tsx src/components/charts/ebi-gauge.tsx` succeeds
    - `grep -c "RadarChart" src/components/charts/meq30-radar.tsx` returns 1+
    - `grep -c "BarChart" src/components/charts/swemwbs-bar.tsx` returns 1+
    - `grep -c "accent-warm" src/components/charts/edi-gauge.tsx` returns 1+ (dark luxury styling)
    - `bun run build` succeeds with no type errors
  </acceptance_criteria>
  <done>
    Four scoring utility functions exported from scoring.ts. Four chart components created in src/components/charts/ using dark luxury palette. All compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comparison route page with dual layout</name>
  <files>
    src/routes/journey/$id/compare.tsx
  </files>
  <read_first>
    src/routes/journey/$id/compare.tsx
    src/hooks/use-journeys.ts
    src/types/journey.ts
    src/components/charts/meq30-radar.tsx
    src/components/charts/swemwbs-bar.tsx
    src/components/charts/edi-gauge.tsx
    src/components/charts/ebi-gauge.tsx
    src/lib/scoring.ts
  </read_first>
  <action>
    Replace the 16-line stub in `src/routes/journey/$id/compare.tsx` with the full comparison view. Per D-07/D-08, desktop uses a two-column grid (charts left, trip summary right), mobile stacks charts first then summary.

    **Structure:**

    1. Use `useJourney(id)` to fetch `FullJourney`. Handle loading/error states per established pattern (`isLoading` → text-secondary loading, `error` → text-danger message).

    2. Back link: `← Back to journey` linking to `/journey/$id`.

    3. Page header: "Your Journey — Before & After" as h1, subtitle "See how your inner landscape has shifted." in text-secondary.

    4. **Score computation:** Import and call:
       - `computeMeq30Subscales(journey.phase2.meq30)` for radar data
       - `computeSwemwbsTotal(journey.phase1.swemwbs)` for before score
       - `computeSwemwbsTotal(latestPhase3.swemwbs)` for after score (if phase3 complete)
       - `computeEdiMean(journey.phase2.edi)` for EDI
       - `computeEbiSum(journey.phase2.ebi)` for EBI

       Where `latestPhase3` = last completed phase3 entry (`journey.phase3Entries.filter(e => e.completedAt).slice(-1)[0]`).

    5. **Dual layout container:** `grid grid-cols-1 lg:grid-cols-2 gap-8 mt-8`

       **Left column (Charts):**
       - Section heading "Instrument Scores" in font-semibold text-lg
       - MEQ-30 Radar in a rounded-[20px] bg-surface p-6 card. Title "MEQ-30 Mystical Experience" in text-sm font-medium. Below chart, if all 4 subscale means >= 3.0, show badge "Complete Mystical Experience" in success color. Per D-10.
       - SWEMWBS Bar in a rounded-[20px] bg-surface p-6 card. Title "SWEMWBS Wellbeing". Pass before/after scores.
       - EDI Gauge in a rounded-[20px] bg-surface p-6 card.
       - EBI Gauge in a rounded-[20px] bg-surface p-6 card.
       - If phase2 is null (no in-trip data), show a message "Complete the Right Now check-in to see your instrument scores." instead of charts.

       **Right column (Trip Summary per D-09):**
       - Section heading "Your Trip Summary" in font-semibold text-lg
       - If `latestPhase3?.tripSummary` exists: render the text in a rounded-[20px] bg-surface p-6 card with text-text-primary leading-relaxed whitespace-pre-wrap.
       - If no trip summary: show placeholder card "Complete the Over Me reflection to receive your holistic trip summary." in text-secondary italic, with a Link to `/journey/$id/phase3/new`.

    6. No animations on page load beyond what chart components handle internally.
  </action>
  <verify>
    <automated>cd /Users/notthatsundar/Code/come-together && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useJourney" src/routes/journey/$id/compare.tsx` returns 1+
    - `grep -c "computeMeq30Subscales\|computeSwemwbsTotal\|computeEdiMean\|computeEbiSum" src/routes/journey/$id/compare.tsx` returns 4
    - `grep -c "lg:grid-cols-2" src/routes/journey/$id/compare.tsx` returns 1+ (dual layout per D-07)
    - `grep -c "tripSummary" src/routes/journey/$id/compare.tsx` returns 1+ (per D-09)
    - `grep -c "Mystical Experience" src/routes/journey/$id/compare.tsx` returns 1+ (per D-10 threshold)
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>
    Comparison view replaces stub with full dual-layout page. Charts on left show MEQ-30 radar, SWEMWBS before/after bars, EDI gauge, EBI gauge. Trip summary on right. Meaningful thresholds highlighted. Dark luxury styling throughout.
  </done>
</task>

</tasks>

<verification>
- `bun run build` passes with zero errors
- `grep -r "computeMeq30Subscales" src/` returns hits in scoring.ts and compare.tsx
- All 4 chart component files exist in src/components/charts/
- compare.tsx contains lg:grid-cols-2 for responsive dual layout
- No default Recharts colors remain (no #8884d8 blue)
</verification>

<success_criteria>
- Comparison view renders instrument score charts (MEQ-30 radar, SWEMWBS bars, EDI gauge, EBI gauge) from real FullJourney data
- AI trip summary displays alongside charts in responsive dual layout
- Meaningful thresholds visually highlighted (MEQ-30 >= 3.0, SWEMWBS delta >= 3)
- All charts use dark luxury palette exclusively
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-comparison-sessions-navigation/06-01-SUMMARY.md`
</output>
